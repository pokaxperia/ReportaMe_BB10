var dibujar = function(){
	//mandas llamar tu canvas para dibujar;
	//path donde se guardo la imagen
	var guardar = "path";

}

var cambiarImagen = document.querySelector('#imagenPredeterminada');
if(dibujar)
	cambiarImagen.src = "file://"+guardar;
else
	//&quea la imagen predeterminadafunctio
function (_path)